services:
  frontend:
    build: ./blog
    ports: 
      - "5173:5173"
    networks: 
      - blogs
    depends_on:
      - backend
  backend:
    build: ./server
    ports: 
      - "3000:3000"
    networks:
      - blogs

networks:
  blogs:
    driver: bridge